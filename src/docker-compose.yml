version: '3'

services:
  db:
    image: postgres
    volumes:
      - ./tmp-dev-db:/var/lib/postgresql/data
  www:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    depends_on:
      - db
    ports:
      - "8000:8000"
    volumes:
      - .:/src
